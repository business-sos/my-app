// Public API for the tile module. Everything else is internal.

export { default as Tile } from './Tile.jsx';
// The Metric @typedef lives in types.js. Consumers can `@import('./tiles/types.js').Metric`
// in their own JSDoc for editor tooltips. No runtime type symbol exists in JS.
